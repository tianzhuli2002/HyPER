defaults:
  - ttH_SL_typed
  - _self_

classification:
  enabled: true

reconstruction:
  enabled: false

loss:
  edge_weight: 0.0
  hyperedge_weight: 0.0
  classification_weight: 1.0

predicting:
  output_mode: classifier
  overwrite: false

probe:
  enabled: true
  pretrained_model_directory: null
  pretrained_checkpoint: null
  trainable_parameter_prefixes:
    - Classification.

optimizer:
  name: adamw
  learning_rate: 1.0e-3
  weight_decay: 0.0

trainer:
  epochs: 50
  checkpoint_mode: keep_best_last
  check_val_every_n_epoch: 1
  limit_val_batches: 1.0

early_stopping:
  enabled: true
  monitor: val_loss
  mode: min
  patience: 10
