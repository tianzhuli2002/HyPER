# Generate the resolved local profile with:
# python tools/create_hyper_representation_profile.py --topology ttbar1L \
#   --output configs/representation_transfer/ttbar1L.env
REP_TOPOLOGY=ttbar1L
REP_RUN_TAG=physics_selection_<campaign>
REP_TITLE='$t\bar{t}$ single-lepton representation transfer'
REP_RECONSTRUCTION_SCORE_DEFINITION='p_tlep * p_thad * p_Wlep * p_Whad'
